<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.association: ingredient, label_html: { class: 'input-color'} %>
  <%= f.input :description %>
  <%= f.submit class: 'btn btn-primary' %>
<%= end %>
